const selectDiaryDate = (state) => state.diary.date;
const selectDiary = (state) => state.diary;
const selectProducts = (state) => state.diary.products;

export { selectDiaryDate, selectDiary, selectProducts };
